# One row per CTest entry, `entry kind gate detail`, read by tests/check_population.cmake before
# tests/verify.cmake runs anything (VM-POP-01, VM-POP-02). A doctest detail is a case floor, a
# minimum at the measured count (VM-POP-05), and a compile-negative detail is the pattern its
# registration must carry. A row counts while its gate is open (ZENGINE_ACTIVE_GATES), and an
# entry's floor sums its open rows (VM-POP-08). ASCII only, with square brackets paired and no
# line ending in a backslash: file(STRINGS) cuts or welds lines at them (VM-PLAT-05, VM-CHECK-03).
smoke                             program           always
neovim                            doctest           always  21
neovim_live                       doctest           neovim  21
# Tripwires in this suite read snake/play.cpp and snake/clock.cpp as text (VM-WALL-11).
snake                             doctest           always  22
timer                             doctest           always  82
# Tripwires in this suite read input/input_sdl.cpp and input/translate_sdl.hpp as text (VM-WALL-11).
input                             doctest           always  50
# A case rides a gate only when it cannot exist without what the gate opens: arithmetic about a
# medium counts in `always`, where every lane proves it.
input                             doctest           sdl     3
surface                           doctest           always  104
surface                           doctest           sdl     7
ui                                doctest           always  22
component                         doctest           always  44
editor                            doctest           always  20
# Tripwires in this suite read workshop/workshop.cpp, workshop/load_execute.hpp,
# operator/source.hpp, operator/migration.hpp and tests/weavelib/operator_consumer_weave.cpp as
# text (VM-WALL-11).
operator                          doctest           always  126
composer                          doctest           always  38
# Tripwires in this suite read files/CMakeLists.txt and workshop/CMakeLists.txt as text
# (VM-WALL-11).
files_weave                       doctest           always  20
# Tripwires in this suite read maker/definition.hpp as text (VM-WALL-11).
maker                             doctest           always  26
# Tripwires in this suite read builder/weave.hpp, builder/runner.hpp and workshop/workshop.cpp as
# text (VM-WALL-11).
builder                           doctest           always  61
workshop_guests                   doctest           always  18
workshop_probe                    doctest           always  14
guest_vocabulary                  doctest           always  3
guest_journey                     program           always
demo_recipes                      program           session
workshop_journey                  program           session
story_journey                     program           session
neovim_journey                    program           session-neovim

workshop_document                 doctest           always  45
workshop_screen                   doctest           always  129
# Tripwires in this suite read workshop/attention.hpp, workshop/pane_definition.hpp and
# workshop/pane_definition_persist.hpp as text (VM-WALL-11).
workshop_panels                   doctest           always  96
# Tripwires in this suite read Workshop's presentation sources, workshop/workshop.cpp,
# workshop/arrangement.hpp, the Introspection, Builder, Info, Terminal and Editor panes' sources
# and CMake files, and tests/weavelib/legacy_pane.cpp as text (VM-WALL-11).
workshop_panes                    doctest           always  849
# Tripwires in this suite read workshop/workshop.cpp and workshop/session_persist.hpp as text
# (VM-WALL-11).
workshop_persistence              doctest           always  156
workshop_load                     doctest           always  151
workshop_editor_switch            doctest           always  11
workshop_neovim                   doctest           always  8
workshop_neovim                   doctest           neovim  26
# Tripwires in this suite read workshop/workshop.cpp, workshop/recipes.hpp and every pane entry's
# bytes (VM-WALL-11).
workshop_files                    doctest           always  59
audit_probes                      doctest           always  4
timer_raw_activation_refused      compile-negative  always  on_timed_activation
timer_activation_hook_compiles    compile-positive  always
timer_missing_using_refused       compile-negative  always  using TimedWeave::on|cannot convert argument 1 from 'const zengine::timer::TimerFired'
timer_missing_using_no_binding_refused compile-negative always using TimedWeave::on|cannot convert argument 1 from 'const zengine::timer::TimerFired'
timer_cursor_bypass_refused       compile-negative  always  (discards|loses) qualifiers
ui_authored_extent_required       compile-negative  always  a resolvable dimension must be an authored Extent
ui_resolved_geometry_refused      compile-negative  always  no resolved rectangle may live on an authored element
ui_authored_element_compiles      compile-positive  always
# The scripts read the source tree, not a build: every configuration runs them, and a deleted
# registration is a red because its row is here.
doc_links                         script            always
package_vocabulary                script            always
law_register                      script            always
source_comments                   script            always
flow                              doctest           always  51

message_draft                     doctest           always  11
source_transfer                   doctest           always  11
source_transfer_cpp               program           always
inventory                         doctest           always  36
flow_pane                         doctest           always  8
