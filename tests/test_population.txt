# The Zengine test-population contract (C4, closing COLD-2 C-4).
#
# This file is the EXPECTATION half of "green means the population Zengine meant to run
# actually ran". It is read by tests/check_population.cmake, which the official lane
# tests/verify.cmake runs before it runs anything else.
#
# WHY IT IS A FILE AND NOT A DERIVATION. Everything else in a build knows what exists; only
# a written expectation knows what is MISSING. Delete `zengine_doctest_test(input ...)` from
# tests/CMakeLists.txt and CTest, the build tree, and every derived list agree instantly that
# there are nine entries and all nine passed -- which is exactly what COLD-2 measured
# (10/10 green after a whole TEST_CASE was deleted; exit 0 after a filter selected nothing).
# A list that is generated from the registrations cannot notice a registration that is gone.
# So this list is not generated from them, and removing one is a red.
#
# THE CONTRACT IS THREE CLAIMS, all fail-closed:
#
#   1. INVENTORY, exact.  For the gates active in this configuration, the set of entries
#      named here must equal, exactly, the set `ctest -N` reports. A missing entry fails; an
#      entry registered and never declared here fails too, and both are named. Adding or
#      renaming a CTest entry is therefore a deliberate act with a line in this file.
#
#   2. KIND.  Each entry's declared kind must match what the registration recorded about
#      itself, because the kind decides which population question is even meaningful. A
#      compile-negative test has no test cases to count and no floor is invented for it.
#
#   3. THE PER-KIND CONTRACT.  Below.
#
# THE KINDS, and what `detail` means for each:
#
#   doctest           A runtime doctest binary. `detail` is a per-suite case FLOOR: the
#                     binary must select at least that many test cases. Floors, never
#                     assertion totals: an assertion total is evidence to report, not a
#                     population, and it is not coverage. No contract file holds one --
#                     nothing would enforce it, so it would decay here unwatched, which is
#                     exactly what the figure that used to sit on this line did.
#
#   compile-negative  A compilation that MUST fail, judged on its diagnostic. `detail` is the
#                     exact PASS_REGULAR_EXPRESSION the registration must carry. Pinned here
#                     rather than merely observed, because "the compiler returned non-zero,
#                     therefore PASS" is the degradation these tests exist to avoid, and it
#                     is one deleted property away.
#
#   compile-positive  The control: the same fixture with the offence removed, which must
#                     BUILD. `detail` is empty, and the registration must carry no
#                     diagnostic pattern -- judging this one on output would let a fixture
#                     that never compiled satisfy it.
#
#   program           An ordinary program with no doctest population (the smoke test). The
#                     contract is that the entry exists, the binary exists, and CTest still
#                     runs the real command. No case count is invented for it.
#
#   script            A repository-owned CMake script run over the SOURCE TREE rather than
#                     over a build -- the documentation-link check and the package-vocabulary
#                     check. `detail` is empty:
#                     there is no binary to exist, no case to count and no diagnostic to
#                     judge, and inventing one of those would be a number that means nothing.
#                     Its registration being declared HERE is the point: the check reads the
#                     tree, so nothing in a build could notice it had stopped being run.
#
# FLOORS ARE MINIMUMS, and the numbers below are a MEASURED baseline (C4's, raised by W-0,
# W-1, W-2, W-3, W-4, W-5, W-6, BLD-0, PNL-0, PNL-1, PNL-2, PNL-2a, ASYNC-1, HD-1 and HD-2 where
# those phases' cases should become permanent), so they carry ZERO downward slack --
# deleting a single test case from any suite crosses its floor -- while leaving healthy
# ADDITIONS free, which is the traffic this repository actually generates. Raise a floor when
# a phase's new cases should become permanent. Do not lower one to make a deletion green: a
# deliberate decrease in evidence is a reviewed edit to this file.
#
# `gate` says WHY a row may be legitimately inactive -- a conditional subpopulation stated
# explicitly instead of hidden behind a slack floor:
#
#   always  every supported test configuration has it; absence is always a defect.
#   sdl     rides the SDL-backed weaves (ZENGINE_SDL_SKIN=ON, the default; the Windows
#           stranger lane turns it off). Absent => declared absent, never "passed".
#
# An entry may carry MORE THAN ONE row, and a doctest floor is the SUM of the rows whose gate
# is active: `surface` is 35 cases everywhere plus 2 behind `#if defined(SURFACE_HAS_SDL)`
# (the SDL skin driven under the dummy video driver, and the same skin refused a video driver
# that does not exist), so its contract reads 37 where SDL is built and 35 where it is not,
# and neither number has any give in it. Since G-1 `input` carries the same split, for the
# same kind of case: a real SDL queue read by a real weave library, and both SDL weaves live
# at once.
#
# THIS FILE IS ASCII, PROSE INCLUDED, AND THAT IS A PARSING FACT AND NOT A STYLE ONE.
# `check_population.cmake` reads it with `file(STRINGS)`, which extracts runs of printable
# ASCII and BREAKS A LINE at anything else -- so one non-ASCII glyph inside a comment splits
# that comment in two and hands the parser the second half as if it were a manifest row. The
# symptom is a fatal error naming a WORD as an entry with an unknown kind (measured, PROJ-0),
# which is loud enough to catch but points nowhere near the cause. Write the notes in ASCII.
#
# G-0's ten new cases are all in the ALWAYS row on purpose, and that placement is the
# claim: the SDL medium's whole label path is pure arithmetic in skin_sdl_plan.hpp, so the
# lane that builds no SDL at all -- the Windows stranger lane -- still proves that a
# SurfaceLabel reaches pixels, in which cell, in which ink, and clipped where.
#
# HD-1 made it a third time, and its split is the sharpest illustration the rule has. A
# bounded text region -- how much prose fits in it, where its pixels are, where its interior
# starts, what a pointer lands on inside it, and what a character medium makes of it -- is all
# pure arithmetic in surface/region.hpp and surface/skin_sdl_plan.hpp, so nine of its ten new
# surface cases are in the ALWAYS row and the Windows stranger lane proves every one of them.
# What rides `sdl` is the single thing that genuinely cannot exist without a font engine: the
# real face opening against a real renderer and answering with a metric it MEASURED.
#
# HD-2 made it a FOURTH time, and this one had nothing left to put behind the gate at all:
# all four of its new surface cases -- a row's ground travelling the cell projection, the
# plan resolving that ground against the region it is in, the bitmap face painting it as the
# cell's own quad, and the terminal medium saying it in SGR -- are in the ALWAYS row, because
# a background behind prose turned out to be entirely a question about arithmetic and bytes.
# The font engine was not involved and no case pretended it was.
#
# G-1 made the same placement decision twice more, and the split is worth reading as one
# rule: EVERYTHING PURE GOES IN `always`. The SDL translation (keys, modifiers, text,
# pointer coordinates, wheel, the close request) is pure code in input/translate_sdl.hpp
# and lands in input's always row; the pixel-to-cell projection is pure code in
# surface/pointing.hpp and lands in surface's. What rides the `sdl` gate is only what
# genuinely cannot exist without SDL -- one case per suite, each driving a real SDL against
# a real weave library.
#
# TUI-0 made the same placement decision a SIXTH time, and its split is the cleanest of them.
# The terminal medium now measures its own terminal, which is the one thing in the phase that
# needs an operating system -- and it is precisely the thing no case asserts a value for,
# because a suite that depended on the developer's window is a suite that fails on a pipe. What
# the `always` row proves instead is everything ELSE: how a measured size becomes a canvas
# extent, that an unmeasurable one becomes silence, that the shell publishes on change and only
# on change, that a shrunken canvas hands back the rows it stopped using, and every one of
# Workshop's reactions to a terminal that grew or shrank. The one case that does call the real
# query asserts only the SHAPE of its answer -- no terminal, or a positive one -- which is true
# on a pty, a pipe and a CI runner alike.
#
# HD-6 put its whole tier in `always` for the reason that keeps recurring here: the Inspector
# body's capacity, its vertical window, its omission markers, its resting-value fitting and
# both projections of its caret are arithmetic over a metric that arrives as a NUMBER, so a
# lane with no font engine proves every one of them by publishing the metric a font engine
# would have measured. The one thing SDL adds is the measurement itself, and `surface`'s sdl
# row has owned that since HD-1.
#
# HD-7 did it again, and its own claim is the sharpest case for the rule so far: the whole
# side-panel composition -- how the two lists share one row budget, that the sharing is
# monotonic in the room, that neither list paints through the other, and that a press names
# the row a maker sees -- is arithmetic over three counts and one metric. Not one of its
# fifteen cases needs a font engine, so the Windows stranger lane proves the entire phase.
#
# HD-9 is the seventh, and it needed no new row anywhere. Its eight cases all sit in
# `workshop`'s always row, because giving a row a GROUND is a published field, a shared cell
# projection and a pure plan -- none of which has ever needed a font engine. The one thing the
# phase genuinely could not assert without SDL is what the slab LOOKS like, and that is a live
# witness in the report rather than a case pretending to be one.
#
# WS-0 raised the workshop floor 333 -> 379, and its split needed no new row anywhere for a
# different reason from the seven above: the phase adds no medium-dependent fact at all. A
# setup is a name and a list of two-string references; resolving one is a comparison, saving
# one is the Loom's own codec, reconciling one is a vector, and the one thing it paints is a
# single line of the bottom band. Not one of its 46 cases needs a font engine or an SDL, and
# the eight that do involve a screen ask the SAME arithmetic question the rest of this suite
# asks -- which is why the Windows stranger lane proves the whole phase.
#
# HD-10 is the eighth, 324 -> 333, and it is the same story once more: where a bounded region
# is PLACED is canvas-cell arithmetic settled before any metric is consulted, so the nine cases
# that pin the reserved column -- disjointness over eleven extents and three metrics, the
# unchanged half-share want, the panel byte-identical with the pane open, and the footer read
# back off the real cell rasterizer -- all sit in the always row. The graphical half is one
# `fit_region` comparison over the same two rectangles, which needs no font engine either.
#
# WP-0 raised the workshop floor 387 -> 431, and it is the FIRST Workshop phase whose
# claim genuinely needs a shared library. Forty-four cases: the protocol's four schemas,
# the descriptor and catalog bounds, the office-provenance negatives, both discovery load
# orders, the windowed combined picker, runtime spatial capacity, the room contract and the
# retained-content bound. Five of them drive `zengine-workshop-hello` -- a real weave loaded
# through the real Kernel and Manager -- which is why this suite now defines a `_SO` path it
# never had; a mock loader would have proved nothing about the ABI it exists to exercise.
#
# It still needed no new row and no gate. The metric half of the phase is one `fit_region`
# comparison over two rectangles, and the loaded fixture is medium-independent by
# construction: it is handed two integers and answers with rows. So the Windows stranger lane
# proves the whole phase, exactly as it proved the eight before it.
#
# (WP-0's own note here read "an overlay slot's rectangle is the same rectangle at every
# extent, so a REAL FACE is the only thing that moves an external pane's prose budget". WIND-1
# ended that: a slot takes half the room's surplus, so a WIDER SURFACE moves the budget too.
# The placement claim moved; the gate argument did not, because a room's share is still
# arithmetic over integers.)
#
# WIND-2 raised the workshop floor 435 -> 473, and it is the TENTH phase to need no new row --
# which is worth reading, because it is by far the largest of the ten. Setup format version 2,
# an authored place and size per axis, a canonical `front` permutation, a recovery inventory
# over seven states, a sixth keyboard mode and a pane gesture with its own custody are
# thirty-eight cases, and not one of them needs a font engine or an SDL:
#
#   the FORMAT is bytes through the Loom's own codec -- three mode words, a version refused by
#   its number before its rows are read, and a round trip that keeps a `pixels` amount nothing
#   in this build can project;
#   the RESOLUTION is canvas-cell arithmetic settled before any metric is consulted (HD-10's
#   law), so the clipping, the off-room rectangle and the per-axis independence are the same
#   integers in both media -- and the two cases that DO vary the metric vary it as a NUMBER,
#   which is how every phase since HD-6 has proved a medium-dependent claim on a lane with no
#   font;
#   the ORDER is a permutation over a vector, and its 10,000-operation bound is a loop;
#   the GESTURES go through the real input path on the real weave, and a terminal press is
#   cells.
#
# So the Windows stranger lane proves the whole phase, exactly as it proved the nine before it.
#
# WIND-1 raised the workshop floor 431 -> 435, and it is the ninth phase to need no new row.
# Four cases: the half-share stated over the whole clamped width domain, the byte-identical
# minimum with its capacity control, both sides of the paint/pointer trade at 200x60 read
# through the live doors, the external grant followed through `fit_region` in both media, and
# the stack/pane overlap swept and bounded. Every one of them is cell arithmetic or a message
# on a bus; the graphical half is the same `fit_region` call with a metric published as a
# NUMBER, so the lane with no font engine proves the phase entire.
#
# WIND-2a raised the workshop floor 473 -> 482 and the surface floors 59 -> 66 (always) and
# 5 -> 6 (sdl), and it is the FIRST phase in a long while to move the `sdl` row at all. The
# split is the same rule read once more, from both ends:
#
#   the ORDER is arithmetic. That a later plane covers an earlier one, kind for kind, in both
#   directions, is a claim about a vector and a rasterizer -- so the seven new always-row
#   cases include the exact terminal bytes, the schema and content-id of `SurfaceLayer v1` and
#   `SurfaceCanvas v5`, and the SDL PLAN's per-plane interleaving with and without a text
#   metric. The lane with no font engine proves every one of them.
#
#   the EXECUTION is not. What the `sdl` row's one new case adds is the thing that genuinely
#   cannot exist without SDL: the real skin, over a real renderer, walking a real two-plane
#   canvas through the one loop that consumes the plan -- and then the same canvas with its
#   planes reversed. It cannot read the pixels back and does not pretend to; what it proves is
#   that the loop runs over planes at all, which is the half a pure plan cannot.
#
# The nine new workshop cases needed no new row for WIND-2's reason, unchanged: an authored
# overlap, a clipped resolved size, a release under a mode, a removed selection, one picker
# inventory, a pixel axis and a hint on row 0 are all cell arithmetic or a message on a bus.
#
# INTR-0 raised the workshop floor 482 -> 504, and it is the ELEVENTH phase to need no new row.
# Twenty-two cases, and the split is the same rule once more with an extra reason of its own:
#
#   WHAT A READING MEANS is a pure function over a value. Parsing the Weave Manager's blob,
#   the last-at-sign rule, an observed empty map, a role the kernel bound nowhere, the row
#   budget swept over four populations and six column widths, the reserved caveat, the counted
#   omission and the marked cut -- none of it touches a bus, a medium or a font, and
#   `introspection/loaded.hpp` links nothing so that this stays true.
#
#   WHERE THE FACT COMES FROM is not pure, and no case pretends it is: eleven of the twenty-two
#   load the REAL `zengine-introspection` shared library through the real Kernel and Manager and
#   read its rows off a published canvas. That is why this suite now defines a SECOND `_SO` path.
#   The two are different kinds of artifact -- `zengine-workshop-hello` is a fixture built by
#   tests/, `zengine-introspection` is a package the host stages and boots -- so a case here that
#   reads a row is reading the shipped tool's own answer.
#
# The medium-dependent case is the usual one and needed no gate: a graphical metric arrives as a
# NUMBER, moves the granted budget, and the projection answers the new room. So the Windows
# stranger lane proves the whole phase, exactly as it proved the ten before it.
#
# TYPE-0 raised the workshop floor 504 -> 510, and it is the TWELFTH phase to need no new row.
# Six cases, and every one of them is medium-dependent in the sense that keeps them in the
# `always` population: the METRIC IS AN ARGUMENT. A case asks for a graphical composition by
# handing `Session` an advance and a line height -- 8 and 18, the numbers this repository's face
# measured -- and `fit_region` answers a row budget. No font is opened, no renderer exists, and
# the SDL skin need not be built: the whole of what a real face changes about a publisher is two
# integers, which is exactly the property HD-1 arranged the seam to have. So the no-SDL lane and
# the Windows stranger lane prove the phase in full, including the pictures a maker only ever
# sees through SDL.
#
# TYPE-1 raised the surface floor 66 -> 71 and the workshop floor 510 -> 517, and it is the
# THIRTEENTH phase to need no new row. Twelve cases, and the split between the two suites is the
# phase's own claim about where a contract lives: `surface` carries the four that are about the
# VOCABULARY -- what an ordinary region does to material under it, what a region whose ground is
# beneath does instead, what a row's own ground means inside one, and that the default is
# byte-for-byte the old behaviour -- and `workshop` carries the seven that are about the one
# CONSUMER that earned the field. A Surface law pinned only in the Workshop suite would be a
# Workshop-shaped proof of somebody else's contract.
#
# EVERY ONE IS IN THE `always` POPULATION, for TYPE-0's reason unchanged: the metric is an
# ARGUMENT. A case asks for a graphical composition by handing a `Session` (or `plan_canvas`) an
# advance of 8 and a line of 18 -- the numbers this repository's face measured -- and
# `fit_region` answers a row budget. No font is opened and no renderer exists, so the no-SDL
# lane and the Windows stranger lane prove the phase in full, including the one thing a maker
# only ever sees through SDL: that the object's material is still under its name.
#
# QR-3 raised the workshop floor 517 -> 521 and moved NO surface floor, which is the phase's
# own claim about where it lives: bounding the object name to the object's material is a
# consumer's decision about its own room, and TYPE-1's Surface contract -- `ground`,
# `kGroundOwn`, `kGroundBeneath` and all three shape versions -- was not touched at all. Three
# FOUR cases, and the count is worth reading beside a fifth that only changed its name:
# TYPE-1's `the name's bound is the WORKSPACE's right edge` became QR-3's `the name's bound is
# the OBJECT'S resolved width`, carrying the ellipsis ladder a widening object walks up. The
# four new ones are the PLANNED-PIXEL witness (no part of a name lands outside the material it
# names), the live one that drives a rename past the OLD bound through the real property editor
# and then resizes the object four times to watch the mark come and go, and two for the
# authored-name bound QR-3 source-traced and moved (32 -> 64, an INPUT BOUNDARY and not a screen
# measurement -- see `doc::kMaxNameLen`).
#
# ALL ARE IN THE `always` POPULATION, for TYPE-0's and TYPE-1's reason unchanged: the metric is
# an ARGUMENT, so the pixel case is proved by `plan_canvas` with no font, no renderer and no SDL
# skin built.
#
# SEL-0 raised the workshop floor 521 -> 549 and moved no other floor, which is the phase's own
# claim about where it lives: a bounded pane press is a Workshop/provider protocol question and
# touched neither Surface nor Loom. Twenty-eight cases in three tiers, and the split is the
# phase's argument in the population file:
#
#   THE SEAM is geometry and provenance. Nine cases drive real pointer events through the real
#   input path into a recording provider seat: the fifth shape walked as a schema, the row/column
#   lattice swept at every row and three columns, the whole rectangle swept in BOTH media with the
#   forwarded coordinate bounded by the grant, the authorship and destination, six kinds of press
#   that must not be forwarded (picker, management, terminal mode, a release, a second button, an
#   unknown space), a pane with no room granted, the bus-tap audit of what Workshop says, and one
#   provider offering two panes.
#
#   THE MEANING is a pure function over a value. Six cases ask what a row of the Loaded view IS
#   -- the entry map, the omission marker as a population fact rather than a hidden weave, marking
#   as a one-row change of fixed width, the marked form still fitting every room over a swept
#   domain, a held selection asking the population rather than the rows, and the literal characters
#   of the mark itself. No bus, no Workshop, no library.
#
#   ...AND THE LAST OF THOSE SIX EXISTS BECAUSE A CANARY FOUND ITS ABSENCE. Every case in this
#   phase says `intro::kSelectedMark`, which is right -- a magic string in twenty places is how two
#   spellings drift -- but a suite in which EVERY reference is the constant cannot notice the
#   constant changing. `> ` -> `* ` was invisible to all 547 cases. One case now spells it.
#
#   THE PRODUCT is the real `zengine-introspection` shared library through the real Kernel and
#   Manager. Thirteen cases: the press that selects what was shown, the same row pressed twice,
#   the non-entry rows, the live omission marker, the SNAPSHOT RACE (a library unloaded under a
#   pane whose rows still name it), a press arriving in the gap between a room grant and its
#   answer, the Manager asked nothing to interpret a press, a forged press from two wrong sides, a
#   row of the previous room, selection held across a resize, a selected library that goes away,
#   the fact carrying no authority, and the same gesture in both media.
#
# EVERY ONE IS IN THE `always` POPULATION, for the reason every phase since TYPE-0 has given: the
# metric is an ARGUMENT. The graphical half of this phase -- a press inside a rendered prose line
# landing on the same logical row although the line height is not the cell height -- is asked for
# by handing `Session` an advance of 8 and a line of 18 and doing arithmetic on the `RegionFit`
# that answers. No font is opened and no renderer exists, so the no-SDL lane and the Windows
# stranger lane prove the phase in full, including the picture a maker only ever sees through SDL.
#
# CAT-0 raised the operator floor 30 -> 53, and its 23 cases needed no new row and no gate for
# a reason none of the phases above could have given: what they prove is which CATALOG INSTANCE a
# running Timer resolved through, and an instance has no medium. The suite gained a third source
# file rather than a fourth CTest entry -- `test_operator_canonical.cpp` compiles into the same
# `zengine-operator-tests` binary -- because the population contract counts ENTRIES, and a phase
# that added an entry for a tier of an existing suite would be splitting the inventory to record
# a file.
#
# The one thing worth reading beside the number: TWENTY of the 23 drive a REAL dynamically loaded
# `zengine-timer` beside OPH-0's real loaded stranger, so this suite now defines the `TIMER_SO`
# path it never had, and one of them reads `workshop/workshop.cpp` as a source file. That last one
# is declared here as what it is -- a tripwire, not a proof -- because Workshop's `main()` claims a
# terminal and no case in this repository can run it.
#
# PROV-0 raised the operator floor 53 -> 88, and its 35 cases needed no new row and no
# gate for CAT-0's reason unchanged: what they prove is WHERE A POWER CAME FROM and which
# contribution currently satisfies it, and a contribution has no medium. The suite gained a
# FOURTH source file rather than a fourth CTest entry -- `test_operator_provider.cpp`
# compiles into the same `zengine-operator-tests` binary -- because the population contract
# counts ENTRIES, and a phase that added an entry for a tier of an existing suite would be
# splitting the inventory to record a file.
#
# What is worth reading beside the number is how much of it is REAL ARTIFACTS. Twenty-six of
# the 35 open at least one shared library across the native module boundary and read operator
# definitions out of it, and SEVEN different images are involved: the shipped
# `zengine-operators-basic` (a production provider that is deliberately not a weave), the
# shipped `zengine-timer` (which exports all three surfaces at once), four provider fixtures,
# OPH-0's loaded stranger, and one ordinary pre-existing weave that provides nothing. Two more
# read `workshop/workshop.cpp` as a source file, and both are declared here as what they are --
# tripwires, not proofs -- because Workshop's `main()` claims a terminal and no case in this
# repository can run it.
#
# LOAD-0 raised the workshop floor 582 -> 635, and it is the FOURTEENTH phase to need no new
# row and no gate -- for a reason none of the thirteen above could have given, because the
# phase has no medium in it at all. What its 53 cases prove is WHICH ARTIFACTS ARE IN THE ROOM,
# and an artifact has no medium: a plan is bytes through the Loom's own codec, a mount is a
# native module boundary, a weave load is a message to the Weave Manager, and a Timer's
# scheduled delay is an integer in a letter. No font, no renderer, no SDL, no cell. So the
# no-SDL lane and the Windows stranger lane prove the entire phase.
#
# The suite gained a SECOND SOURCE FILE rather than a second CTest entry --
# `test_workshop_load.cpp` compiled into the same `zengine-workshop-tests` binary -- for the
# reason CAT-0 and PROV-0 both gave next door: the population contract counts ENTRIES, and a
# phase that registered an entry for a new tier of an existing suite would be splitting the
# inventory in order to record a file. (It is its own `workshop_load` entry now, and that
# reasoning still holds: what changed was not the tier, it was that one binary had become the
# repository's whole compile and test floor. The note above the table has it.)
#
# WHAT IS WORTH READING BESIDE THE NUMBER is how much of it is REAL ARTIFACTS. Twenty-eight of
# the 53 open at least one shared library across the native module boundary, and SIX images are
# involved -- the shipped `zengine-operators-basic` and `zengine-timer` (the two the production
# plan actually names), SEM-0's substitution instrument, an ordinary pre-existing weave, a
# provider at an ABI this host does not speak, and a consumer at one it does not speak either.
# They are STAGED INTO ONE DIRECTORY by the suite itself, because "filesystem presence is not
# load authority" cannot be measured against a lookup table: the directory deliberately holds
# more artifacts than any plan in the file names.
#
# CAUTION -- AND THIS SUITE NOW LOADS THE TIMER, which its CMake comment said it deliberately did not.
# The hazard that comment measured is real and unchanged -- a live Timer re-arms its own beat
# inside its own handler, so a rig that pumps to EMPTY never returns. Every rig in the new file
# is a LOCAL of its case with its own Switchboard and Kernel, and every drain is
# `pump_pending()` in bounded turns. Do not hoist one to file scope.
#
# QR-4 raised the workshop floor 680 -> 683 and moved no other floor, which is the repair's own
# claim about where it lives: two maker-facing sentences in the Powers pane's shared projection,
# no operator semantics, no pane protocol and no medium. THREE cases, and they are permanent
# because a wording defect is exactly the kind a green cannot see -- the old rows were
# well-formed strings that a suite was happy to assert, which is how `1 providers` survived a
# whole phase and how a sentence claiming a law about every weave survived beside a pane that
# had read one catalog.
#
# Two of them ask the PROJECTION what a heading says at each cardinality the pair can take, and
# the third reads the same two repairs back off the PUBLISHED CANVAS -- because a projection
# that is right and a pane too narrow to say it are different results, and `fit` marks that
# difference with an ellipsis no tier-one case can see. All three are in the `always` row for
# INTR-1's reason unchanged: a projection is pure arithmetic over a value, and the live one
# spends the character medium, so the no-SDL lane and the Windows stranger lane prove them all.
#
# QR-6 raised the workshop floor 683 -> 685 and moved no other floor, which is the repair's own
# claim about where it lives: the suite's STAGING FIXTURE, no load-plan semantics, no provider
# mounting, no pane and no medium. TWO cases, and they are permanent because the defect they pin
# is one a green cannot see from inside a single run. `tests/load-plan-stage` sits in the build
# tree, nothing deletes it, and the second run of that binary in one build tree
# stages over its own previous output -- which on MinGW was refused `file_exists` by libstdc++'s
# same-file guard (its `stat` reports st_ino 0 for every file), turning 683/683 into 642/683
# with no source change between the two runs. Linux and MSVC never reproduced it.
#
# The two are a pair on purpose. The first asks whether the repeat is ALLOWED; the second asks
# whether it MEANS anything, by changing the source between the two calls and requiring the
# destination to follow. A repair that merely stopped reporting the error would pass the first
# and leave the previous run's artifact in place, which is the failure that does not announce
# itself -- so the second case is the one carrying the contract, and the first is the one that
# names the symptom a developer actually hit. Both are in the `always` row because staging is
# a filesystem copy: no gate opens or closes over it.
#
# QR-9 raised the workshop floor 685 -> 693 and moved no other floor, which is the repair's own
# claim about where it lives: the load ADAPTER's decision that a load conversation has settled,
# no load protocol, no operator semantics, no pane and no medium. EIGHT cases, and they are
# permanent because both defects they pin are ones a green cannot see. Measured on the source
# before the repair: a `zen.Result` from an unrelated participant made a plan report success
# naming WeaveId 424242 -- the number that participant chose, which no Kernel ever minted -- and
# the same message turned a MISSING artifact's refusal into a completed plan; an unrelated
# `zen.Refused` failed a load that had in fact succeeded. Every one of those runs was green.
#
# The eight split three ways. FOUR drive the real `zen.LoadWeave` on a real Kernel with an extra
# participant speaking the shared answer vocabulary, and they are the arms that fool the old
# adapter. THREE work a focused fixture whose respondent DEFERS its answer (Loom's ANS-02), which
# is the only way to hold a conversation open with an empty queue: one of them is the empty-turn
# falsifier proper -- a bounded turn delivers zero work, `pending()` reads 0, the answer is owed,
# and it arrives afterwards -- one is the correlation half's own falsifier, where the speaker IS
# the respondent and only the number is wrong, and one drives the production executor at a
# respondent that never answers, so the FUSE expires and must read as a local guard rather than
# as a refusal anybody made. The eighth is the sender half's: a stray that guesses the
# correlation, because the first conversation on any fresh host is 1 and a correlation is not a
# secret.
#
# All eight are in the `always` row: this is bus traffic and plain arithmetic over a record, so
# the no-SDL lane and the Windows stranger lane prove them all. They needed no new CTest row --
# `test_workshop_load.cpp` compiled into the same `zengine-workshop-tests` binary -- and they
# obey that file's standing rule unchanged: every rig is a LOCAL of its case with its own
# Switchboard, and every drain is `pump_pending()` in bounded turns.
#
# BOOT-0 raised the workshop floor 693 -> 715 and the operator floor 89 -> 90, and it moved no
# other floor -- which is the phase's own claim about where it lives: the OWNERSHIP of an
# authored project's realization, and nothing about a pane, a medium, a load protocol or an
# operator's meaning.
#
# The old executor performed a whole plan inside one call, because the continuation of a weave
# load was a stack frame; to hear its own answer it turned the bus itself, 64 dispatch turns per
# load. That fuse is deleted rather than moved: the owner is persistent, it issues one load and
# RETURNS to the host, and the load's own correlated answer wakes it through the participant that
# already heard one. So for the first time there is an observable state between "not started" and
# "finished", and TWENTY-TWO of the twenty-three new cases are about that middle.
#
# The workshop twenty-two split six ways. TWO are the boundary itself -- `begin()` comes back with
# a row in flight and says exactly where, and a plan that owes nobody anything finishes inside it
# with the queue untouched. ONE is the interruptibility witness: ordinary traffic already queued
# when realization begins is NOT delivered by the time it returns, and twenty more deliveries land
# while a row hangs on a respondent that defers forever. TWO are authored order -- three mixed rows
# walked turn by turn with "at most one in flight" as the falsifier, and an overlay that must not
# mount early. TWO are the operator offer's custody across host turns, whose instrument is a
# REFUSAL rather than a number (with the Timer's own provider mounted, this host's rule and the
# artifact's local copy are the same rule and answer identically). TWO are `loaded` is not `live`:
# the Kernel has the artifact strictly before the row settles, and a direct `Kernel::load` bypass
# produces a registered Timer that never breathes. The remaining THIRTEEN are the refusal path
# mid-flight, the stray-answer wall re-measured now that a stray could drive a whole plan, the
# four-state arrangement read at four points on the timeline, and the fuse's own case rewritten as
# what is true without it: an unanswered load stays unanswered, and nothing mints a third answer.
#
# The operator ONE is a source tripwire and is declared as what it is: it reads
# `workshop/load_execute.hpp` with the prose stripped and refuses ten scheduler verbs and nine
# async nouns, and reads `workshop/workshop.cpp` for plan-specific control flow in a host loop.
# A property that is true because of what code does NOT contain cannot be proved by running it;
# the behavioural half is the interruptibility witness above, and BOTH go red together under a
# canary that puts the 64-turn loop back.
#
# All twenty-three are in the `always` row: bus traffic, a catalog, a Kernel and plain arithmetic
# over a record -- so the no-SDL lane and both Windows lanes prove every one. They need no new
# CTest row (`test_workshop_load.cpp` compiled into the same `zengine-workshop-tests` binary) and
# they obey that file's standing rule unchanged: every rig is a LOCAL of its case with its own
# Switchboard, and every turn any plan in this suite takes is spent by the CALLER.
#
# WUX-0 raised the workshop floor by twenty, and all eighteen are in the `always` row for
# the reason every prior phase's were: taking a desk back is a question about a FILE and about
# what a weave does with one, and there is not an SDL call anywhere in it. What the graphical
# medium contributes to this feature -- creating a window from a picture and growing it to a
# later one -- is the medium's own documented behaviour and is witnessed by hand against a real
# window, not here; what IS here is the invariant that behaviour rests on, and it is pure: the
# FIRST canvas of a run is the floor extent and the restored room is a later one. A lane with no
# SDL at all proves that ordering, which is the half a canary can break.
#
# BLD-1 raised the builder floor 30 -> 48 and the workshop floor 735 -> 754, and it is the
# FIRST phase since BLD-0 to move the builder row at all. The eighteen new builder cases split
# three ways: the RECIPE's law (what a maker may write down and what is refused), the ARTIFACT
# question (a process exiting zero is not a file, a stale file cannot make a failed build look
# successful, a neighbour satisfies nothing), and the GENERATED PROJECT read as text -- which
# is the only way a claim about what a single-source build does NOT reach into can be checked
# on every lane rather than on the one that happens to have a compiler configured for it. The
# nineteen new workshop cases are the realization half: a row a host says is WAITING, the one
# door that performs it later, and the Builder panel's two outcomes.
#
# BLD-1a raised the workshop floor 754 -> 761 and moved no other floor, which is exactly the
# repair's own shape: it changed no build mechanism, added no participant and touched no
# vocabulary the Builder suite measures -- it changed what realization is allowed to do with an
# answer it already had. Seven net new workshop cases, and they split three ways. FOUR are the
# barrier itself, one per thing a later row could otherwise have done: mounted a provider,
# loaded a weave, been reported `resolved` in the Project projection, or let `Complete` stand
# beside a row nobody performed. TWO are the door: a later authored row may be BUILT early and
# is refused realization by the name of the row it is behind, and a refused realization puts
# the frontier back where the ask found it so a corrected build is a retry. ONE is the host
# seam: realization now comes to REST more than once in a run, and a host that is not told
# about the waiting rest point has no line to print for a maker whose project is short an
# artifact.
#
# The OPERATOR row did not move and gained an assertion group instead: the same source tripwire
# that refuses ten scheduler verbs and nine async nouns in `workshop/load_execute.hpp` now also
# refuses seven filesystem spellings there. A frontier that STOPS makes `is the file there yet?`
# tempting, and that question is the host's -- asked once per row, never polled.
#
# BLD-2 raised the workshop floor 761 -> 767 and moved no other floor, which is the phase's own
# shape: it added no build mechanism, no realization rule and no message on the bus -- it made
# the frontier BLD-1a already keeps VISIBLE and ACTIONABLE on the Builder panel. Six cases.
# ONE is the phase's sharpest falsifier: the panel's `project` row read off the published
# canvas while the owner's frontier MOVES underneath it, three times, with nobody notified --
# an implementation that copied the frontier at any point on the path shows the old stem and
# goes red. TWO are the `f` gesture's honest arms: the single producing recipe is selected
# visibly and asked for through the existing Build & Realize route (a `chosen`-spending or
# first-entry mutation reddens it), and TWO recipes producing one artifact -- authored law,
# `check_recipes` deduplicates identities and deliberately not artifacts -- refuse the gesture
# until the maker picks with `c`, THE FIRST CATALOG ROW MATCHING on purpose, so "use entry
# zero" and "read `chosen`'s default as a choice" both send an ask this case forbids. TWO are
# the refusals in words (nothing waiting; nothing here produces the frontier) and the unbound
# key without the panel. ONE is a source tripwire beside INTR-1's: the presentation files
# spell no realization-owner type, no offer, and no runner order -- awareness of the frontier
# must not quietly become authority over it. All six are in the `always` row: the frontier is
# a value, the join is a string comparison, and the canvas is cells, so the no-SDL lane and
# both Windows lanes prove every one.
#
# WARNING: THE BUILDER SUITE NOW DRIVES A REAL CONFIGURED CMAKE TREE (tests/buildfixture/), because
# BLD-1 deleted the suite's ability to invent a command: a recipe names inputs to a mechanism
# and there is no field anywhere in which one could name a program. The fixture is configured
# at BUILD time and only `cmake --build` runs inside a case, which is the same line
# `tests/package/run.cmake` draws for the same reason -- a nested CONFIGURE inside a ctest
# entry is a second build system in the population, and a nested BUILD is what a recipe IS.
# The suite's wall clock roughly doubled and that is the cost of measuring the production path.
#
# TEXT-0 raised four floors at once -- component 15 -> 33, surface 71 -> 78 (always), input
# 35 -> 42 (always), workshop 767 -> 775 -- and the spread IS the phase's shape: a component
# earned selection, clipboard, history and the one key vocabulary (component); a region may
# carry a selected range and each medium answers in its own voice (surface); every backend can
# now produce the editing keys with measured modifiers, and the SDL reader routes the
# platform's clipboard fact (input); and Workshop's four editable places reach all of it, with
# `^c` meaning copy exactly where text has the keyboard and quit exactly where it cannot
# (workshop). EVERY ONE IS IN THE `always` POPULATION, for the reason every phase since TYPE-0
# has given: the metric is an ARGUMENT, a selection span is arithmetic, a clipboard sequence is
# bytes, and the one live-library case drives the real zengine-composer through the real seam
# with no medium anywhere near it. The composer suite's floor did not move, which is that
# file's own doctrine: it holds the PURE half, and the fourth consumer's live behavior is a
# workshop-tier claim.
#
# QR-11 moved four floors -- component 33 -> 34, input 2 -> 3 (sdl; the always row traded
# one deleted TEXT-0 case for the new pin and stands), surface 78 -> 81 (always) and
# 6 -> 7 (sdl), workshop 775 -> 780 -- and the spread is the repair's shape: clipboard READ
# follows paste intent now, everywhere the read exists.
# TEXT-0's ambient road (the SDL reader publishing every platform clipboard change, and the
# pre-existing clipboard on first poll) is retired WHOLE: the shape that carried it no longer
# exists, the reader's sources are tripwired clean (input, always row -- a claim about what
# code does NOT contain is read off the source on every lane), and the event class is pinned
# in the ignored set. What replaced it is a conversation: a consumed Ctrl+V records a request
# on the owner's Clipboard (component), the owner asks the Skin, the Medium answers the
# platform's CURRENT text or the honest cannot-say (surface -- the fake pins exactly-once
# and both answers; the new sdl-row case reads the REAL SDL clipboard through the real skin,
# seeded and changed by the process the way any unrelated application would), and the text
# lands in the draft that asked or nowhere (workshop -- the five new cases are the injection
# wall, the stale-mirror falsifier, the draft-boundary discard, the carried-draft delivery,
# and the no-skin quiet degradation). The one input sdl-row addition is SC-1's live witness:
# clipboard seeded before the reader existed and changed behind the running application, and
# neither payload can reach the bus, said in the strongest form available -- the ears hear
# every shape the reader can emit and a tap watches every delivery by name.
#
# WHAT IS NOT IN THIS FILE, DELIBERATELY: the single-source route's REAL compile. It needs an
# installed Zengine prefix and a toolchain, it is a lane and not a ctest entry
# (tests/build/run.cmake), and folding it in here would put an install and two nested projects
# inside a test binary's population. What IS here is every part of that route a suite can hold
# still: the recipe law, the command derivation, and both generated files as bytes.
#
# Keep this file plain ASCII: it is read with file(STRINGS), which treats a non-ASCII byte as
# a string terminator and would silently split the line around it. Two more characters the
# parser owns: `#` starts a comment (so a diagnostic pattern cannot contain one), and `;` is
# CMake's list separator and is refused outright rather than being eaten halfway through.
#
# WUX-2 raised the workshop floor 799 -> 810 and the surface always row 81 -> 86, and its
# split is the fine lattice's own claim: geometry that a maker can drag by a pixel is still
# ARITHMETIC. The five surface cases pin the sub-cell conversions (exact, floored, total,
# saturating, with the wire's remainder decomposition and its garbage-reads-as-zero rule),
# the one quantization law as a property at BOTH shipped grains (a span lands on device
# units by flooring both edges, and the hit answer is that presentation read backwards), a
# fine rect through the SDL plan and the terminal rasterizer, a fine label at both grains,
# and a fine region's fit/projection/press-inverse agreeing about one viewport. The eleven
# workshop cases are the behavior: a one-pixel drag moves a pane by one pixel of lattice
# through the REAL pointer path; all eight resize handles preserve their opposite anchor
# (the reported top-edge defect, reproduced at START, is pinned dead); a refused anchored
# resize writes neither place nor size; right/bottom resizes keep a default place reactive;
# hit meets exactly the painted pixels at a fractional edge; the TUI projects a fine pane
# onto its covered cells and rewrites nothing; setup v2 and session v1 load through the
# retained legacy shapes onto the exact lattice; fine geometry round-trips the v3 file; and
# introspection says a fine value as an exact reduced fraction. EVERY ONE IS IN THE `always`
# POPULATION, for the reason every phase since TYPE-0 has given: the metric and the grain
# are ARGUMENTS -- a pixel-space press is a number with a stamp, so the no-SDL lane and the
# Windows stranger lane prove the whole phase, including the smoothness a maker only ever
# feels through SDL (whose live half is the phase's witness, not a case).
#
# KEY-0 raised the workshop floor 780 -> 799 and the component floor 34 -> 35, and it is the
# phase whose eighteen workshop cases are all about ONE value: the keymap. Exact matching
# behaviorally falsifies the retired subset aliases (ctrl+n, alt+q, ctrl+shift+s); the
# terminal toggle's shift+space default is gone and pinned gone; the hotkey view opens,
# closes, swallows, and lists three genuinely different contexts; an authored override moves
# dispatch and every displayed spelling together, survives a restart, and dies with its file;
# admission refuses a same-context collision naming both claimants, admits cross-context
# reuse, preserves an unknown action's row byte-for-byte, and holds the two global walls
# (bare printables, the component's own chords); a known POSIX gap is accepted and said; and
# the printable-trigger swallow is derived from the binding, capitals included. The component
# case is one sweep with both directions in it: every (scancode, modifiers) pair in the named
# space is consumed by `TextBox::consume` exactly when `kEditingVocabulary` declares it, so
# the declaration rows contextual help shows and the switch that executes cannot drift apart.
#
# WUX-3 raised the workshop floor 810 -> 832 and the surface always-floor 86 -> 94, and the
# spread is the phase's own split. The twenty-two workshop cases are the installed
# application: the per-user roots (both platform spellings, pinned on every lane), the one
# precedence (explicit path, then --isolated, then the default -- with an unresolvable root
# an absence, never CWD), the one-time legacy import (imports once into an absent
# destination, never overwrites an existing root file, never deletes the original,
# converges by existence), the host tripwire holding main() to that resolution, the prefs
# file (round-trip, closed words, written on the toggle, applied before the first paint,
# never overwriting a file it could not read, and the WUX-1 focus law surviving a RESTORED
# hidden-titles preference), session format v3 (the placement's words judged, its
# coordinates accepted as another machine's desktop truth, the v2 and v1 roads retained),
# and the weave's placement custody (remember opaquely, offer back once at restore, retain
# across a placement-less run, and keep the NORMAL room gated while this run's medium says
# maximized). The eight surface cases are the medium's half: the shell's change-guarded
# placement report with silence for the absence, placement-before-extent at every door,
# the offered-back delegation, and the pure adaptation law (verbatim for reachable intent,
# partial overhangs included; the exact 32px grasp boundary in both axes; clamp into the
# nearest current display; top-left alignment for an outsized window; and NO answer with
# no display truth -- a blind move refused). EVERY ONE IS IN THE `always` POPULATION: the
# roots are pure functions over an environment value, the placement law is pure geometry,
# and the weave cases drive published messages -- the live Windows window is the phase's
# witness, not a case.
#
# WUX-4 raised the workshop floor 832 -> 849 and the surface always-floor 94 -> 96, and the
# spread is the phase's own split. The seventeen workshop cases are the DISTINCTION between a
# thing that happened and a thing that is true: a held condition that stands until its owner
# retracts it and outlives every sentence said over it, a derived one that enters and leaves
# attention because its subject's state moved (with no retraction call anywhere on the path),
# the judgement about which true pane states earn a glance and which belong in their own
# contextual projection, the frontier's waiting condition, dismissal proved
# presentation-only against a file on disk that is still refused afterwards, the re-arm on a
# materially changed statement (each field of the statement on its own), dismiss != resolve
# and resolve != dismiss pinned separately, an ordinary event sentence that does not become a
# condition, an alert that opens no mode however many beats pass, a condition that names an
# action and cannot execute one, the ranked compact line with its honest count, the view and
# its projection through KEY-0's help surfaces, and the source tripwire over the condition
# model itself, and the bound witness beside it -- the view's published row count swept against
# the room's own answer at three extents by twelve cursor positions, because a region PADS what
# it was not given and SILENTLY DROPS what will not fit, so an over-spending painter loses
# exactly the omission marker that existed to say so. The two surface cases are the MEDIA's half: what the SDL plan makes of the
# attention slot in the picture (a region in the real face, its box sized from the metric,
# the cell fallback, the narrow-canvas clamp, and NOTHING at all for an empty slot) and the
# terminal's own row-2 answer including the erase that retracts it.
#
# EVERY ONE IS IN THE `always` POPULATION, and this phase is the sharpest case for that rule
# yet: a condition is a value with no wire form, the projection is a pure function of a
# session, the ranking is arithmetic, and the chip is a `SurfaceLayer` composed in canvas
# cells -- so the lane that builds no SDL at all proves the graphical indicator's geometry,
# its ink, its fallback and its retraction. What genuinely needs a window is that a human can
# SEE it, which is the phase's live witness and not a case.
#
# ONE EXISTING SUITE CLAIM MOVED RATHER THAN GREW. `notes.back()` used to mean "the status
# line", because a repaint published exactly one `SurfaceText`; it publishes two now, so the
# cases that read it ask BY SLOT (`status_note`, `attention_note`). That is a fixture repair,
# not new evidence, and it is named here because a reader comparing floors would otherwise
# have to find it in a diff.
#
# ONE WORKSHOP ENTRY BECAME SIX, AND THE SUM IS THE INVARIANT. `test_workshop.cpp` had
# grown to one thirty-thousand-line translation unit behind one CTest entry: thirty seconds
# of compiling before an assertion could run, and a nineteen-second test CTest could only
# ever hand to one worker. It was cut along the headings its own source already had --
# document, screen, panels, panes, persistence -- beside `test_workshop_load.cpp`, which was
# already a second file in that one binary and is now a sixth entry of its own.
#
# EVERY CASE MOVED VERBATIM. The floors below are the measured populations of the six, and
# they sum to the population the single `workshop` entry ran (863) plus the one case the
# split itself owes: that a `TempDir`'s root is named for the suite that made it, which is
# what keeps six concurrent binaries out of one directory. What this file protects is
# unchanged -- a deleted registration is still a red, a deleted case still fails its suite's
# floor -- and it now protects it six times, once per area, so a deletion names the area it
# happened in instead of moving one number by one.
#
# CTX-0 raised four of the six workshop floors -- panels 139 -> 149, document 134 -> 139,
# screen 169 -> 172, panes 180 -> 182 -- and needed no new row, which is the family's own
# split doing its job: the contextual-action surface's twenty cases land in the area each
# one proves. Panels holds the surface itself (subject capture, the declared populations,
# spending on the pointed pane, admission before selection, the truthful absence refusal,
# `manage.remove` by key and by menu, navigation, input containment, mode precedence);
# document holds the explicit-id deletion and its selection-repair pin, the live-draft
# hold-back, the document-replacement invalidation and the keymap admission of the two new
# identities; screen holds the painted-where-hit inverse pair and the generated claim
# surfaces; panes holds the seam silence -- a right press over a provider's pane, and every
# gesture spent on the open surface, crossing the pane seam not at all.
#
# The source editor added a SEVENTH workshop entry, `workshop_editor`, with its own floor:
# a new area of the product (the multiline buffer, the source-byte law, the tab geometry,
# the Builder's edit-source door, save/dirty truth, the viewport, the deferred-paste
# settlement and the no-silent-loss floor) gets a suite of its own, exactly as the family
# split intends, so a deleted case names the area it went missing from. The six existing
# floors are untouched: every editor case is new evidence, and the two example-gesture
# updates inside `workshop_document` (a keymap override illustration moved off the letter
# the edit-source door now binds) and `workshop_screen` (the picker-census fixture now
# counts three built-ins) moved no floor.
#
# EDIT-1 added an EIGHTH workshop entry, `workshop_files`, for the same reason -- where
# source comes from is its own area -- and its floor was written at 20 while the suite
# shipped 31, which is eleven cases of the downward slack this file's own rule forbids.
# PROJ-0 closes it: `workshop_files` 20 -> 36 and `builder` 48 -> 51, no new row, no gate.
# The five files cases are the completed catalog's OWNER (the derived view, replacement by
# contents rather than by object, the edit-source closure asked of the owner, the two-base
# decoy re-proven over it, and the host read as a source file); the three builder cases are
# the two participants that read it -- the same-object check, the behavioural falsifier that
# changes the catalog in one place and asks both weaves what they answer, and the
# compile-time refusal of a temporary. NOTE: the eleven cases the old floor was not
# protecting were EDIT-1's, not this phase's: raising the number to the measured count
# is the correction, and lowering it again would be a deliberate decrease in evidence.
#
# PROJ-2 takes `workshop_files` 54 -> 73, no new row and no gate: the browser stopped being
# the project. Nineteen cases across four subjects -- the location representation (one
# absolute spelling, the lexical parent, the measured filesystem fixed point), the location
# MARKS (their owner, dedup across provenance, host-reported roots, the three gestures, the
# durable file and its per-row admission), the two seams that had to keep NOT moving (an
# external source through the one editor door, an external catalog whose relative source is
# still the project's file), and the path presentation both measured consumers now need. ALL
# NINETEEN ARE IN THE `always` ROW AND RUN ON BOTH FAMILIES; only three ASSERTIONS are
# per-platform (the drive-letter shape of a Windows root, the drive-root fixed point, and
# the Windows spelling of an absolute location), behind `#if defined(_WIN32)` inside cases
# that run everywhere -- a case compiled out on one family is a floor that cannot protect it
# there.
#
# QR-12 takes `workshop_files` 49 -> 54, no new row and no gate: the admission boundary that
# stops a filesystem path this application cannot say from ending the process. Two cases are
# the boundary itself (an ordinary path and an ordinary name are carried unchanged; the
# launch capture is the working directory when it can be said) and three are the hostile
# conditions -- an unsayable filename is one inert row rather than the end of the listing, it
# refuses at the live door, and a launch directory that cannot be said is the designed
# absence. ALL FIVE ARE IN THE `always` ROW AND ALL FIVE RUN ON BOTH FAMILIES. Only the
# ARRANGEMENT is per-platform (ill-formed UTF-16 on NTFS, arbitrary bytes on POSIX), behind
# one helper, deliberately: a case compiled out on one family is a floor that cannot protect
# it there, which is the same downward slack the paragraph above is about.
#
# SOURCE-1 raised the panes floor 182 -> 220, and the thirty-eight new cases are the shape
# of a pane that stopped being a projection and became a browser. They are in the `always`
# row for the reason every phase since TYPE-0 has given: the metric is an ARGUMENT. A case
# asks `project_powers_ui` for eight rows by forty-eight columns and gets the terminal
# default; it asks for four by seventy-one and gets the graphical one; neither needs a font
# engine, an SDL, or a medium of any kind, and both measured budgets are asserted rather
# than assumed.
#
# TWO OF THEM ARE A DIFFERENT KIND OF EVIDENCE, and the floor cannot say so -- which is why
# it is said here. `browsing the catalog runs no evaluator at all` and `a cross-image Source
# proves the sample is an evaluation, not a lookup` are ARRANGED COUNTERS, not assertions
# about text: the first brackets every browsing gesture between two deliberate samples of a
# body that counts its own spends and requires the difference to be exactly one; the second
# requires three consecutive samples to answer three consecutive numbers. A constant-
# returning fixture would have made an accidental evaluation invisible, and a memoised one
# would have made a cached answer indistinguishable from a fresh evaluation -- so
# `zengine-provider-source` joins this suite's fixture list, and `op::invocations()` alone
# is deliberately not trusted to answer a question about another image.
#
# WUX-5 raised the screen floor 172 -> 187, and the nine new cases are the desk that
# stopped being a set of undifferentiated rectangles. They are together in `workshop_screen`
# because they are one claim -- what a maker SEES and what their hand REACHES are the same
# thing -- and each names the mutation it falsifies in its own comment: the border deleted,
# the border painted but not spent, paint or hit walking the authored order instead of the
# effective one, the selected style collapsing into the ordinary one, the help going back to
# the window corner, the menu's heading or its hint returning, the removed rows still setting
# the menu's width, the press inverse keeping an offset the painter no longer spends, a popup
# wearing the pane's own chrome, a closed pane keeping its lift, and a pane teaching a key
# again. ALL NINE ARE IN THE `always` ROW: not one needs a font engine or an SDL -- the
# metric is an argument, and both media's budgets are asserted from it. The thirteen
# mutations were RUN, one at a time, against the whole lane; the matrix is in
# `Zen/reportbacks/WUX-5-evidence.md`.
#
# THE OTHER SEVEN WORKSHOP FLOORS DID NOT MOVE, and that is the honest reading rather than an
# oversight: WUX-5 changed presentation, so what it cost those suites was re-pinning what a
# pane's rows and a pane's granted room now measure -- not new cases.
#
# WUX-6 moved four floors -- surface 96 -> 98 (always), workshop_screen 187 -> 190,
# workshop_panes 220 -> 226, workshop_persistence 127 -> 129 -- and the spread is the shape
# of a phase whose subject is ONE authored value read through two faces:
#
#   surface (2)         the conversion itself. `device_of_subs` is `px_of_subs` with the
#                       layout number taken from whatever the medium REPORTED, swept against
#                       the shipped face's own answer over every value in four cells;
#                       `subs_exact_in_device` is the other half -- whether that medium can
#                       say the authored number at all -- swept rather than sampled, with
#                       the round trip an exact answer claims required in both directions.
#                       Beside them, the two media's own reports: a terminal's permanent
#                       zero, and the shipped face's canvas laid out at `kCanvasCellPx`.
#   workshop_screen (3) the spelling and the custody. The unit word, the mark, the whole
#                       rectangle and the authored row in both faces; that Workshop holds
#                       only what a medium said and invents nothing when nobody spoke; and
#                       the falsifier for the whole phase -- two sessions identical but for
#                       which medium spoke to them agree on every rectangle and every cell
#                       of `occupied_at`, and differ only in the SENTENCE.
#   workshop_panes (6)  the maker's side. The arrangement notice in each face and the
#                       authored value surviving the crossing; where a pane the maker did
#                       not place actually is; the coarse step through the existing resize
#                       door, moving no other pane and meeting the same per-axis refusal;
#                       its discoverability in the keymap and the help rather than in pane
#                       chrome; and the DESK-LEVEL Compose witness -- six body rows and no
#                       fields at the developer's default, the whole form and its Submit
#                       after one grow, `composer/view.hpp` byte-identical.
#   workshop_persist(2) that looking is not authoring. A geometry that cannot round-trip
#                       through a terminal is authored, a whole session is spent LOOKING at
#                       it through both media, and the file that closing writes is compared
#                       BYTE FOR BYTE against one written by a session that never crossed.
#                       Beside it: the medium's device unit reaches no durable file, and a
#                       restore hands this run's unit back rather than resetting it.
#
# WUX-7 moved five floors -- component 35 -> 39, workshop_screen 190 -> 198,
# workshop_panels 155 -> 157, workshop_files 82 -> 85, workshop_panes 226 -> 227 -- and the
# spread is the shape of a phase that made three ordinary interactions ordinary:
#
#   component (4)       what a WORD is, and that there is one definition of it. The two run
#                       scanners are swept against the two keyboard walks at every position
#                       of a line with runs, single and double separators; the span answer is
#                       pinned at both edges of a word, on the separator that ends one, at the
#                       end of the text and between two separators; `select_word_at` is shown
#                       to produce the component's ORDINARY selection (copy, replace, drag);
#                       and `ctrl+shift+Left` and a double-click are shown to select the same
#                       bytes because both spend the same scan.
#   workshop_screen (7) the two temporal and geometric laws, and Arrange. Double-click
#                       qualification stated as a pure predicate with every one of its five
#                       conditions falsified; the Terminal line's gesture end to end, with
#                       the interval boundary itself pinned in both directions; the revealed
#                       row's width, marks and clamp swept over every width and offset; the
#                       pointer column -> offset map's monotonicity and totality; the four
#                       agreements a scrolled row needs; and the OVERLAP falsifier for
#                       contextual Arrange -- the addressed pane rather than the topmost one
#                       becomes selected, lifts, receives the pointer and is what the
#                       arrangement keys move, with the authored bytes unchanged.
#   workshop_panels (2) the second instance of one component, and the first hover consumer.
#                       A property draft's double-click, its cross-box and cross-word
#                       falsifiers and its modifier exemption; and an object row read past
#                       its ellipsis -- at rest, at the far edge, on every neighbouring row,
#                       under a pane drawn over it, and with nothing underneath it moved.
#   workshop_files (2)  the long PATH, which is the value this canvas has cut longest. The
#                       location row revealed inside its own rectangle and restored on
#                       leaving; a re-measured pane; one listed name against a neighbour
#                       that fits; the chrome and the rows past the listing; and the proof
#                       that pointing re-enumerates nothing -- a file appearing on disk
#                       under a resting pointer stays absent until a maker asks.
#
# THREE OF THEM ARE MASK-CLOSERS RATHER THAN NEW CAPABILITY, and they are worth naming
# because each was found by a mutation the suite did NOT catch, and each turned out to pin
# a claim an EARLIER phase had made and only three-quarters proven:
#
#   workshop_screen  the arrangement desk's pointer walk really does spend the effective
#                    order (WUX-5's fourth consumer). Making it read the authored order was
#                    green until a LIFTED pane was put under the arrangement pointer in an
#                    overlap it does not authoredly own.
#   workshop_files   the hover row inverse really is `files_row_of_body_row`. Replacing it
#                    with `row N is entry N` was green until a listing was SCROLLED, which
#                    is the only arrangement where the two numbers differ.
#   workshop_panes   an arrow really does step the AUTHORED sub-unit value. Flooring the
#                    proposal to the medium's own lattice was green until an off-lattice
#                    place was nudged on a medium that cannot say it (WUX-2a/WUX-6).
#
# THESE ARE ALSO ALL `always`, for WUX-6's reason: a pointer position is an ARGUMENT
# here, and what an SDL cannot be asked in a headless lane -- whether a real window reports
# hover at all -- is a live witness rather than a case (`Zen/reportbacks/WUX-7-RB.md`).
# NOTE: the rig now owns the monotonic reading every press is stamped with
# (`InteractionClock`, tests/workshop_support.hpp), and its default step is past
# `kDoubleClickMs`: a case that publishes two presses means two deliberate aims unless it
# says otherwise, which is what every pre-WUX-7 case already meant by them.
#
# ALL THIRTEEN ARE IN THE `always` ROW, and the `sdl` rows did not move. Not one of them
# needs a font engine or a window: the medium's device unit is an ARGUMENT here exactly as
# the text metric has been since TYPE-0 -- a case says what the medium reported and reads
# what Workshop then says. What an SDL cannot be asked in a headless lane is whether a real
# window reports it, and that is a live witness rather than a case
# (`Zen/reportbacks/WUX-6-RB.md`).
#
# WUX-8 raised the surface floor 98 -> 100 (`always`) -- one for this phase's own case, and one
# that re-baselines a case an earlier phase left above its floor -- the workshop_screen floor
# 198 -> 205 and the workshop_panes floor 227 -> 230, and moved no other floor, which is the
# phase's own claim about where a boundary's THICKNESS lives. One case is Surface's
# (`subs_of_one_device`: the smallest span a medium can show, the other direction of WUX-6's
# `device_of_subs`), seven are the screen's pure geometry (which unit each face pays, that the
# graphical ring is one device pixel INSIDE the pane, that the interior it no longer reserves
# comes back as capacity, that a face which describes an interior in CELLS pays the cell, that
# the ring is still the backdrop the body did not cover, that a content-sized surface still
# reserves the coarsest boundary, and that the picture across a selection change differs in INK
# alone), and three are the panes suite's: the border/body press inverse swept at exact WINDOW
# PIXELS, the granted room across a selection change, and a medium switch that must rewrite
# nothing authored.
#
# ALL ARE IN THE `always` POPULATION, for TYPE-0's reason unchanged and one more of WUX-8's own:
# a face is described to this code by what a medium REPORTS -- a room, a text metric, and a cell
# size in device pixels -- so a case asks for the shipped window by handing `Session` those three
# numbers. No font is opened, no renderer exists, and the no-SDL lane and the Windows stranger
# lane therefore prove the pixel-thin boundary in full. What they cannot prove is that a real
# window PAINTS it, which is what the phase's live SDL witness is for.
#
# WUX-9 moved four floors -- workshop_screen 205 -> 218, workshop_panels 157 -> 164,
# workshop_panes 230 -> 233, workshop_persistence 129 -> 133 -- and the spread is the shape of
# a phase whose whole delta is one vector of an existing value plus the gestures that exchange
# it:
#
#   workshop_screen (16) the shelf as a VALUE and the tab run as a COMPOSITION, which are the
#                        two halves that own no bus and no session. The four value operations
#                        swept over every destination in both directions (the mutation that
#                        SWAPS rather than re-inserting passes the first hop and reorders on
#                        the second, so the sweep is the falsifier); the derived visible
#                        window with the live tab always painted, both omission markers, the
#                        cut live tab, and the spans proven disjoint and inside the text; and
#                        the press inverse over EVERY cell of every painted tab, every other
#                        band row, the status to the right of the run, and the naming state
#                        where no tab is painted at all. TWO of the fifteen are mutation
#                        answers rather than plan: the run's column bound as a PROPERTY over
#                        every population and every width from 0 to 120 (which found the
#                        omission markers being appended AFTER the budget was spent), and
#                        `new` pressed from the middle of a run rather than from its end
#                        (which is where every other case stood, because that is where `new`
#                        leaves you -- and a `push_back` of the original is correct there and
#                        swaps two layouts anywhere else). A THIRD came from the live TUI
#                        witness rather than from either: the saved marker's reservation has
#                        to cover the mark `detail::fit` spends on cutting the row, or a
#                        full-budget run at 78 columns leaves a maker reading `| UNSA...` --
#                        swept over every population, every name length and every live
#                        position the minimum screen can hold.
#   workshop_panels (7)  the live half: the four command-mode gestures with their researched
#                        defaults and their POSIX deliverability, the switch returning
#                        membership + geometry + front order byte for byte over three round
#                        trips, the Workshop-global facts a switch may not touch (selection
#                        retained but resolving to nothing, no ghost lift, the document and
#                        the browser's place unmoved), the pointer's own door, the proof that
#                        a tab press is not a press on a pane, and the modes that already own
#                        their bare keys.
#   workshop_panes (3)   the provider floor: one pane in two layouts is one runtime row, one
#                        presentation and one granted room with the provider told NOTHING
#                        across four switches; a withdrawal reaching the provider not at all
#                        and costing exactly one grant on the way back; and an inactive
#                        layout's rows staying dormant while the catalog gains and loses
#                        nothing of theirs.
#   workshop_persistence (4) what the shelf means for the two files -- `s` writing the live
#                        layout alone, `r` restoring into it alone (refusal included), the
#                        session still carrying ONE desk at version 3 with the shelf absent
#                        next run, and the cross-medium byte-identity falsifier one level
#                        above WUX-6's: two layouts, two media, four crossings, every
#                        authored number unchanged.
#
# ALL ARE `always`, for WUX-6's and WUX-8's reason unchanged: what a medium reported is an
# ARGUMENT here, so both faces are exercised without a font or a window. What a headless lane
# cannot ask -- whether a real terminal and a real window paint and answer this run -- is the
# phase's live witness pair (`Zen/reportbacks/WUX-9-RB.md`).
#
# QR-14 moved ONE floor -- workshop_screen 221 -> 225 -- and re-baselined a great many
# assertions without adding a case to any other suite, which is the shape of a repair that
# moves a band rather than adding a capability. The four new cases are the move's own claims:
# the selector is canvas row 0 on both media and no tab is painted in the footer; the reserved
# chrome was RE-HOMED and not added to (`kTopRows + kBottomRows == 6`, and `room_h` swept
# against `h - 6` at every height from the minimum to 120); every owner of where the body
# begins agrees -- the stack, the side region, the overlay column, `occupied_at`, a real press
# and `workspace_cell_y`, each asked separately rather than through the constant they share;
# and the tab inverse swept over EVERY cell of the screen, answering on row 0 and nowhere
# else, with the painted cells and the answering cells proven to be the same set.
#
# WHAT ELSE MOVED WAS ARITHMETIC, and it is worth naming because it is the whole risk of this
# repair: about fifty assertions across five suites named a canvas row, a rectangle or a band
# row index. Each was re-derived from the constant that owns it rather than re-numbered by
# hand where that was possible (`kSideY + kChromeCells`, `kStackY`, `kWorkspaceY`,
# `workspace_row`), so the next band that moves moves them too.
#
# TWO MEASURED FACTS CHANGED RATHER THAN THEIR SPELLING, and both are consequences of the
# body starting one row lower:
#
#   the Info column   is `room_h` cells tall now rather than one more than that, because the
#                     side region begins under the full-width top band. `share_body_rows`
#                     spends the smaller budget by its own policy: the minimum TUI panel
#                     seats five object rows and five property rows where it seated five and
#                     six.
#   the stack/pane    overlap is TWO rows at the minimum screen where it was one, and one row
#                     at the two heights above it where it was none -- the stack is anchored
#                     to the workspace's top and the pane to the screen's foot. Still bounded,
#                     still measured, still unrepaired for HD-10's own stated reason.
#
# entry                           kind              gate    detail
smoke                             program           always
snake                             doctest           always  22
timer                             doctest           always  82
input                             doctest           always  42
input                             doctest           sdl     3
surface                           doctest           always  100
surface                           doctest           sdl     7
ui                                doctest           always  22
component                         doctest           always  39
operator                          doctest           always  125
composer                          doctest           always  35
builder                           doctest           always  53
# QR-13 MOVED NO FLOOR, AND THAT IS THE WHOLE POINT OF RECORDING IT HERE. `workshop_panes`
# stopped being one translation unit and became five (_seam, _window, _input,
# _introspection, _sampling) because one MinGW Debug object could no longer name all of that
# suite's instantiations. It is still ONE CTest entry running ONE binary over the SAME 233
# cases, every one of them moved byte-for-byte -- so the number below is unchanged, and that
# is the claim: a compiler bound is not an evidence bound, and a repair that had to lower
# this line would have been the wrong repair. What the split does change is what a shortfall
# now means: a source dropped from the suite's SOURCES list still BUILDS and still LINKS,
# and the only thing that notices is this floor. Measured, with one of the five omitted:
# `FLOOR: workshop_panes selected 220 cases, below declared floor 233`.
#
workshop_document                 doctest           always  140
workshop_screen                   doctest           always  229
workshop_panels                   doctest           always  164
workshop_panes                    doctest           always  233
workshop_persistence              doctest           always  148
workshop_load                     doctest           always  115
workshop_editor                   doctest           always  66
workshop_files                    doctest           always  85
audit_probes                      doctest           always  4
timer_raw_activation_refused      compile-negative  always  on_timed_activation
timer_activation_hook_compiles    compile-positive  always
timer_missing_using_refused       compile-negative  always  using TimedWeave::on|cannot convert argument 1 from 'const zengine::timer::TimerFired'
timer_missing_using_no_binding_refused compile-negative always using TimedWeave::on|cannot convert argument 1 from 'const zengine::timer::TimerFired'
timer_cursor_bypass_refused       compile-negative  always  (discards|loses) qualifiers
ui_authored_extent_required       compile-negative  always  a resolvable dimension must be an authored Extent
ui_resolved_geometry_refused      compile-negative  always  no resolved rectangle may live on an authored element
ui_authored_element_compiles      compile-positive  always
doc_links                         script            always
package_vocabulary                script            always
